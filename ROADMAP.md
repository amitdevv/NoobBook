# NoobBook Roadmap

Tracked on the GitHub project board: **<https://github.com/users/yashranaway/projects/2>**

- Open items live as issues in `TeacherOp/NoobBook` with the `roadmap` label.
- Status (Todo / In Progress / Done) is the source of truth on the board, not this file.
- PRs auto-link to their issue via `Closes #N` — completion flips the card to Done.

To list open work from the CLI:

```sh
gh issue list --repo TeacherOp/NoobBook --label roadmap --state open
```
