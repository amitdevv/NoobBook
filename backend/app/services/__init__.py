"""
Services package initialization.

The service layer contains business logic,
separate from the API layer (which handles HTTP) and the data layer
(which handles storage). This separation makes the code more testable
and maintainable.
"""